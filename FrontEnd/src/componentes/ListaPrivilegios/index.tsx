'use client'

type Props = {
  curso:number,
  nombre: string,
  tipo:string,
  grupo:string,
  name:string 
};

export const ListaPrivilegios = ({ curso, nombre, tipo, grupo, name, }: Props) => {
  

  return (
    <div>

    </div>
  );
};